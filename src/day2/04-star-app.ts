import { random } from "introcs";
import { forward, left, setSpeed, setOnClick, moveTo, turnTo } from "introcs/turtle";

export let main = async () => {
    // TODO #1
    
};

let star = (points: number, diameter: number, angle: number): void => {
    // TODO #0

};

main();