import { print, promptString, random } from "introcs";

export let main = async () => {

    print(answer(0));
    
};

let answer = (n: number): string => {
    // TODO #1: Return different answer strings based on number n
    return "TODO";
};

main();